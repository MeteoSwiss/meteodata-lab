name: docs

on:
  push:
    branches:
    - main

jobs:
  build:

    runs-on: ubuntu-latest

    permissions:
      contents: write
      pages: write

    steps:
    - name: Checkout current repository
      uses: actions/checkout@v4
    - name: Install poetry
      run: pipx install poetry
    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: 3.12
        cache: poetry

    - name: Install poetry and python package
      run: poetry install --with docs --all-extras

    - name: Build documentation
      env:
        VERSION: ${{ github.ref_name }}
        GITHUB_SHA: ${{ github.sha }}
      # Set the buildId to the first 7 digits of the commit hash
      run: BUILD_ID=$(echo ${GITHUB_SHA::7}) poetry run sphinx-build docs docs/_build/

    - name: Publish documentation
      uses: peaceiris/actions-gh-pages@v3

      with:
        publish_branch: gh-pages
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: docs/_build/
        keep_files: true
